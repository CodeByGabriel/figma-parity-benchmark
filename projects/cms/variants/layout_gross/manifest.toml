id              = "layout_gross"
category        = "layout"          # 1 das 6: color | layout | alignment | component | image | text
severity        = "gross"          # gross | subtle
expected_status = "mismatch"
injected_by     = "manual"
description     = "colunas de conteudo empilhadas em vez de lado a lado"

# medido por diff contra o v0 (mesma formatacao): 2 linha(s) alterada(s), 1 arquivo.
# o defeito e a unica diferenca funcional contra reference/index.offline.html;
# o prefixo dos caminhos de assets difere pela posicao na arvore do repositorio.
files_changed = [
    "index.html",
]
