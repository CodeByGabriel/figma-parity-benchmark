id              = "color_subtle"
category        = "color"          # 1 das 6: color | layout | alignment | component | image | text
severity        = "subtle"
expected_status = "mismatch"
injected_by     = "manual"
description     = "KPI Cards: gradiente de fundo trocado (token -> tom vizinho)."

# medido por diff contra reference/ :
files_changed = [
    "section-cards.tsx",   # modificado
]
