id              = "layout_subtle"
category        = "alignment"          # 1 das 6: color | layout | alignment | component | image | text
severity        = "subtle"
expected_status = "mismatch"
injected_by     = "manual"
description     = "KPI Cards: espacamento da grade gap-4 -> gap-8."
# Nota: mudanca de gap e espacamento SEM reestruturacao -> categoria 'alignment' no gabarito, apesar do nome da variante.

# medido por diff contra reference/ :
files_changed = [
    "section-cards.tsx",   # modificado
]
