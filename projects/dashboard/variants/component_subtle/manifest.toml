id              = "component_subtle"
category        = "component"          # 1 das 6: color | layout | alignment | component | image | text
severity        = "subtle"
expected_status = "mismatch"
injected_by     = "manual"
description     = "KPI Card Growth Rate: badge de tendencia removido."

# medido por diff contra reference/ :
files_changed = [
    "section-cards.tsx",   # modificado
]
